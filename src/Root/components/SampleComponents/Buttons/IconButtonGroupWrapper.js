import React from 'react';

const IconButtonGroupWrapper = ({children}) => {
    return (
        <div className="btn-group ml-auto">{children}</div>

    );
};

export default IconButtonGroupWrapper;